#include <stdio.h>
#include <stdlib.h>
#include "random.h"

int main(int argc, char** argv)
{
	if (argc != 2)
	{
		printf("Modo de uso: ./solver test.txt\n");
		return 0;
	}

	// Codigo

	return 0;
}
